# Tuples are immutable collection of unordered data's
# Duplicates are allowed in both the list and tuples
my_tuple = ('a', 'b', 'b', 'a')
print(my_tuple)

# Set are mutable data type in which duplicates are not allowed
my_set = set(['s', 'a', 's', 'b'])
print(my_set)
